import 'package:flutter/material.dart';

class EditorConfig {
  static const double fontSize = 14.0;
  static const double lineHeight = 1.5;
  static const double linePixelHeight = fontSize * lineHeight;
  static const String fontFamily = 'monospace';
  static const double topPadding = 4.0;
  static const double bottomPadding = 16.0;

  static const TextStyle textStyle = TextStyle(fontFamily: fontFamily, fontSize: fontSize, height: lineHeight);
}
